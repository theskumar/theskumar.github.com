+++
title = "Blog"

# The writing index now lives at the site root (/). Don't render a separate
# /blog/ list; the home page aliases /blog/ -> / as a redirect.
[build]
render = "never"
list = "never"
+++
